## 1. Stylesheet

- [x] 1.1 Add `.btn-icon` CSS class for icon-only buttons (compact padding, no text decoration, consistent size)

## 2. Spool List Actions Column

- [x] 2.1 Add View icon button (`👁` or inline SVG) as an `<a>` link to `/spools/:id` in the actions cell
- [x] 2.2 Replace "Edit" text anchor with an icon button (`✏`) linking to `/spools/:id/edit`
- [x] 2.3 Replace "Delete" text button with a trash icon button (`🗑`) — keep the `confirm_delete` signal logic
- [x] 2.4 Replace "Sure?" confirm button with a trash/check icon button and retain "Cancel" as a small icon or minimal text button

## 3. Spec Sync

- [x] 3.1 Update `openspec/specs/spool-management/spec.md` with the modified "Spool list UI" requirement from the delta spec
